export default class Account {
    constructor(public id: number, public mail: string, public description: string, public password : string, public name : string, public historique : string, public url: string) {
    }
  }